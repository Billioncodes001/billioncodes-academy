# Bundled Brand Assets

`icon-192.png` and `icon-512.png` are exact copies of the parent-owned original Billion Codes vector raster exports in `public/brand/`. The 192px mark is rendered by React Native Image in the header; the 512px mark is configured as the Expo app icon. No third-party logo or remote runtime asset is used.
